# Airflow services now rely on the official apache/airflow base environment.
# Project runtime dependencies are installed into the isolated venv configured in:
# - requirements.txt
# - docker/airflow/requirements-project-runtime.txt
