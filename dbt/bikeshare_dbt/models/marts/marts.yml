version: 2

models:
  - name: dim_station
    columns:
      - name: station_key
        tests:
          - not_null
          - unique
      - name: city
        tests:
          - not_null
      - name: station_id
        tests:
          - not_null
      - name: capacity
        tests:
          - non_negative
